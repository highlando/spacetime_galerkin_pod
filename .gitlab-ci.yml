before_script:
  - apt install --assume-yes python-dev python-pip
  - python -V  # Print out python version for debugging
  - pip install nosetest

job1:
  script:
    - cd tests
    - nosetests
  only:
    - mass-mat-hosvd
