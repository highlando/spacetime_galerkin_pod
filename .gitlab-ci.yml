before_script:
  - apt install python3
  - apt install pip
  - python3 -V  # Print out python version for debugging
  - pip install nosetest

job1:
  script:
    - cd tests
    - nosetests
  only:
    - mass-mat-hosvd
