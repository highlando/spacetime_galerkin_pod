before_script:

  image: "docker-registry.csc.mpi-magdeburg.mpg.de:5000/ci-images/minimal/ubuntu:bionic-python3"
  - python3 -V  # Print out python version for debugging
  - pip3 install nosetest numpy scipy

job1:
  script:
    - cd tests
    - nosetests
  only:
    - mass-mat-hosvd
