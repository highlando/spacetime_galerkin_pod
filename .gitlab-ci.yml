before_script:
  - apt install --assume-yes python3
  - apt install --assume-yes pip
  - python3 -V  # Print out python version for debugging
  - pip install nosetest

job1:
  script:
    - cd tests
    - nosetests
  only:
    - mass-mat-hosvd
